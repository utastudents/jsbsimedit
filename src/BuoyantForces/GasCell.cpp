#include "GasCell.hpp"

// GasCell constructor
GasCell::GasCell() : Component() {
    name = "Gas Cell";
    // volume = v;
    // temperature = t;
    // pressure = p;
    // mass = m;
}

GasCell::~GasCell() {
    // Destructor definition
}